#include <stdio.h>
#include <vector>
#include <math.h>
using namespace std;

vector<int> inputTable[5050];

float funcabs(float num) {
	if (num > 0)
		return num;
	else
		return -1 * num;
}

int main() {

	unsigned int n;
	
}