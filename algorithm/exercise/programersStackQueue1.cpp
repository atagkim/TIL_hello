#include <string>
#include <vector>

using namespace std;

vector<int> layerTable[10010];

vector<int> solution(vector<int> prices) {
    vector<int> answer;



    return answer;
}

int main() {


    vector<int> prices = { 1,2,3,2,3 };

    vector<int> revalue = solution(prices);
    for (int i = 0; i < revalue.size(); i++) {
        printf("%d ", revalue[i]);
    }

}